// Global test setup
// You can add global mocks, configurations, or initializations here
// For example, mocking environment variables or setting up a test database

// Example: Mocking an environment variable
// process.env.TEST_VARIABLE = 'test_value';

// Example: Setting up a global beforeAll hook
// beforeAll(() => {
//   console.log('Global setup: Runs once before all tests');
// });

// Example: Setting up a global afterAll hook
// afterAll(() => {
//   console.log('Global teardown: Runs once after all tests');
// });
